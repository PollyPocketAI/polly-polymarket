# ── Polymarket CLOB API credentials ──
# Derived from holding wallet private key via create_or_derive_api_creds()
POLYMARKET_API_KEY=
POLYMARKET_API_SECRET=
POLYMARKET_API_PASSPHRASE=

# ── Holding wallet (cold storage, main funds) ──
# Cosmi sends USDC here. Also used as CLOB API signer.
HOLDING_WALLET_PUBLIC=
HOLDING_WALLET_PRIVATE=   # NEVER commit or share this

# ── Polymarket trading wallet (funder/proxy) ──
# This is the wallet displayed on polymarket.com
# USDC.e must be deposited here on Polygon for trades to execute
POLYMARKET_WALLET_ADDRESS=
POLYMARKET_WALLET_PRIVATE=  # Optional: allows Polly to bridge funds automatically

# ── GitHub ──
GITHUB_TOKEN=
GITHUB_USERNAME=PollyPocketAI
GITHUB_REPO=polly-polymarket

# ── Betting config ──
MAX_BET_PCT=0.03       # Max % of bankroll per bet (Kelly-capped)
MIN_EDGE=0.08          # Minimum |Polly_P - market_P| to trigger a bet
MIN_CONFIDENCE=0.60    # Minimum confidence score to trigger a bet
